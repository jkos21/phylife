{
  "name": "phylife",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run graph:seed && vite",
    "build": "npm run graph:seed && tsc && vite build",
    "preview": "vite preview",
    "graph:seed": "npx tsx scripts/seed-disk.ts",
    "graph:refresh": "npx tsx scripts/refresh-graph.ts",
    "pipeline:sync": "node --experimental-strip-types scripts/sync.ts",
    "pipeline:seed": "node --experimental-strip-types scripts/seed.ts",
    "pipeline:stats": "node --experimental-strip-types scripts/stats.ts",
    "test:persisted-kg": "npx tsx scripts/test-persisted-graph.ts",
    "test:backend": "npx tsx scripts/test-backend-integrations.ts",
    "test:walkthrough": "npx tsx scripts/test-ui-walkthrough.ts",
    "test": "npx tsx scripts/test-persisted-graph.ts && npx tsx scripts/test-backend-integrations.ts && npx tsx scripts/test-ui-walkthrough.ts"
  },
  "dependencies": {
    "lucide": "^0.475.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
